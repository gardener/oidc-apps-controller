configuration:
targets:
  - name: "nginx"
    labelSelector:
      matchLabels:
        app: nginx
    configuration:
      oidcCABundle: Y2VydGlmaWNhdGUK
      oauth2Proxy:
        clientID: "test-client-id"
